{
  "name": "@lean4/repo",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "watch": "lerna run --parallel watch",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.3.4"
  }
}
